# Release v2.2.1

## Overview

v2.2.1 fixes a bug where `--no-config` had no effect and `.snpconfig.json`
was always loaded and merged when present.

## Bug Fixes

### `--no-config` now works correctly

`LoadConfig` never checked the `NoConfig` parameter, so `.snpconfig.json` in
the source directory was loaded and merged into every run regardless of the
flag. The following now works as expected:

