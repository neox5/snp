# Release v2.0.1

## Overview

v2.0.1 replaces the third-party `go-gitignore` dependency with a purpose-built
pattern matching implementation, fixing silent filter failures for patterns like
`*.test.js` and `**/*.test.js`.

## Bug Fixes

### Pattern matching now works correctly

The previous implementation using `go-gitignore` silently failed to match
user-supplied patterns against deep paths. Both of the following now work as
expected:

