# Release v2.2.0

## Overview

v2.2.0 fixes a fundamental traversal bug where `--exclude-all` combined with
`--include` patterns targeting subdirectories silently produced no output.
The fix required a full redesign of the internal matcher architecture.

## Bug Fixes

### `--exclude-all --include <path>` now works correctly

When `--exclude-all` was set, directory traversal was pruned before reaching
any target path. The following patterns now work as expected:

