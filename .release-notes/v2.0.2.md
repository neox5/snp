# Release v2.0.2

## Overview

v2.0.2 fixes a bug where user-supplied `--include` and `--exclude` flags were
silently dropped when no baseline flag was present.

## Bug Fixes

### `--include` and `--exclude` now work without a baseline flag

When using the implicit default traversal mode, user-supplied filter rules were
never applied. Both of the following now work as expected:

